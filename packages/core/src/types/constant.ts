export enum EventTypes {
  LIFECYCLE = 1,
  ERROR,
  PERFORMANCE,
  API,
  DOM,
  ROUTE,
  CONSOLE,
  RECORD,
  VUE,
  CUSTOMER,
  EXTEND,
  WHITE_SCREEN,
  XHR,
}

export enum ErrorTypes {
  RESOURCE_ERROR = 1,
  JS_ERROR,
  API_ERROR,
  UNKNOWN_ERROR,
}
