export enum EventTypes {
  LIFECYCLE = 1,
  ERROR,
  PERFORMANCE,
  CLICK,
  CONSOLE,
  RECORD,
  XHR,
  API,
  // VUE,
  // CUSTOMER,
  // EXTEND,
  // WHITE_SCREEN,
}

export enum ErrorTypes {
  RESOURCE_ERROR = 1,
  JS_ERROR,
  API_ERROR,
  UNKNOWN_ERROR,
}
